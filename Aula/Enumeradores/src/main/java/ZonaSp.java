
// Ennums são tipos especiais de classes onde definimos um conjunto pequeno e limitado de valores
public enum ZonaSp {
    // Criamos um tipo de dado chamado ZonaSp que só pode ter os valores abaixo:
    LESTE,
    OESTE,
    CENTRO,
    NORTE,
    SUL

    // A vantagem disso é garantir que não tenha divergências na escrita do código, padronizando o código
    // Pode ser usado, por exemplo, para estados, como: Aceite, Entregue e etc
}
